name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # أو master حسب اسم الفرع الرئيسي عندك

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build Project
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # استبدلها بـ build إذا كنت تستخدم Create React App وليس Vite
          branch: gh-pages